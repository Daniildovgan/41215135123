#include <iostream>

int main() {
    int a = 10, b = 20;
    int *ptr_a = &a;
    int *ptr_b = &b;

    int max = (*ptr_a > *ptr_b) ? *ptr_a : *ptr_b;

    std::cout << max << std::endl;

    return 0;
}
