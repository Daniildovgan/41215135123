#include <iostream>

int main() {
    const int N = 5;
    int arr[N] = {1, 2, 3, 4, 5};

    int *ptr = arr + N - 1;

    while (ptr >= arr) {
        std::cout << *ptr-- << " ";
    }
    std::cout << std::endl;

    return 0;
}
