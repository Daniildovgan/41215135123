#include <iostream>

int main() {
    const int N = 5;
    int arr[N] = {1, 2, 3, 4, 5};
    int sum = 0;

    int *ptr = arr;
    for (int i = 0; i < N; ++i) {
        sum += *ptr++;
    }

    std::cout << "Сума елементів масиву: " << sum << std::endl;

    return 0;
}
